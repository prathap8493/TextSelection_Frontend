import { api_url, website_url } from "@/config/config.js";

export const appInfo = {
  appName: "TextSelection",
  apiDomain: api_url,
  websiteDomain: website_url,
  apiBasePath: "/auth",
  websiteBasePath: "/auth",
};
